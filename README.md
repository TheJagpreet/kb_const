# KB Const

A Python project using LangGraph.

## Setup

Use uv to manage the environment.

```bash
uv venv --python 3.13
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
uv pip install -r requirements.txt
```

## Run

```bash
python main.py
```